# ECサイト概要資料

本資料は、現在の EC サイトの実装状況をコードベースに沿って整理したものです。主な目的は、以下の 2 つです。

- 一般会員が商品を閲覧・検索し、カートに追加できる購入導線を理解する
- 管理者が商品・在庫・ユーザー・売上の状況を管理できる管理画面を理解する

> 本資料では、実装済みの機能を「現状」、未接続または UI のみの処理を「今後の拡張」として整理しています。

## 1. サイトの位置づけ

このプロジェクトは、Next.js App Router を使った EC サイトの実験・開発用アプリケーションです。認証、商品一覧、商品詳細、カート、配送先管理、管理者用ダッシュボードまでが実装されており、実際のオンラインショップとしての基本機能を備えています。

実装の中心は次の 3 つです。

- 一般会員向けの購買導線
- 管理者向けの運用画面
- Prisma + PostgreSQL を使ったデータ管理

特に `src/proxy.ts` でパスごとの認証・権限制御が行われており、保護対象のページには未ログイン時のリダイレクトが入っています。

## 2. 利用者と権限

| 利用者 | 主な機能 | 主な入口 |
| --- | --- | --- |
| 一般会員 | 商品検索、詳細閲覧、カート追加、配送先確認、パスワード変更 | `/products/list` |
| 管理者 | ダッシュボード確認、商品管理、ユーザー確認、在庫・販売状態管理 | `/dashboard` |

権限の判定は `users.admin` フラグを使っています。

- 一般会員が `/dashboard` などの管理者ページへアクセスしようとすると `/products/list` へリダイレクト
- 管理者が一般会員向けページへアクセスすると `/dashboard` へリダイレクト
- 未ログイン時は `src/proxy.ts` により `/login` へ誘導

## 3. 画面構成と遷移

### 3-1. 共通認証

- `/` → `/login` にリダイレクト
- `/login` → 一般会員ログイン画面
- `/login/admin` → 管理者ログイン画面
- `/signup` → 会員登録画面

ログイン・登録ではメールアドレスと 8 文字以上のパスワードが検証されます。パスワードは `bcrypt` でハッシュ化され、JWT は `httpOnly` の `auth_token` Cookie に保存されます。

### 3-2. 一般会員向け画面

| 画面 | 内容 |
| --- | --- |
| `/products/list` | 商品の一覧表示、検索、カテゴリ絞り込み、ページネーション |
| `/products/list/[id]` | 商品詳細の表示 |
| `/cart` | カート内商品の確認、数量変更、削除、合計表示 |
| `/account` | 配送先情報の確認・更新 |
| `/account/password` | パスワード変更画面への導線 |

一般会員は商品一覧から詳細へ遷移し、数量を選んでカートへ追加します。購買処理そのものの決済や注文確定は未接続で、現時点では UI 上の導線に留まっています。

### 3-3. 管理者向け画面

| 画面 | 内容 |
| --- | --- |
| `/dashboard` | 売上、注文数、商品数、会員数、在庫アラートを表示 |
| `/products/management` | 商品検索、一覧、登録 |
| `/products/management/[id]` | 商品の更新、販売状態変更、削除 |
| `/users` | 会員一覧の確認 |

管理者向けの一覧画面では、商品名・カテゴリ名・ユーザー名で検索でき、ページ単位で表示されます。商品登録時は `name`、`category`、`price`、`count` を検証し、重複商品や重複カテゴリの登録を防いでいます。

## 4. 主要な業務フロー

### 4-1. 一般会員の購買フロー

1. `/signup` または `/login` で会員登録・ログイン
2. `/products/list` で商品を検索・絞り込み
3. 商品詳細で説明と在庫を確認
4. 数量を選択してカートへ追加
5. `/cart` で数量と合計を確認
6. 購入ボタンを押す流れを想定するが、現状は UI と仮の導線のみ

カート追加時には、商品が販売中であるか、在庫数を超えないかの検証がサーバー側で行われます。同一商品を追加した場合は数量が合算されます。

### 4-2. 管理者の運用フロー

1. `/login/admin` でログイン
2. `/dashboard` で売上と在庫の状況を確認
3. 商品管理画面で商品を登録・更新・削除
4. 在庫数や販売停止状態を管理
5. 必要に応じてユーザー一覧から会員情報を確認

## 5. データモデルと保存内容

データベースは Prisma で定義されており、接続先は PostgreSQL を想定しています。主なモデルは以下の通りです。

| モデル | 役割 |
| --- | --- |
| `users` | 会員・管理者の基本情報と認証情報 |
| `address` | 配送先住所・電話番号 |
| `products` | 商品名、価格、在庫数、説明、販売状態 |
| `category` | 商品カテゴリ |
| `product_images` | 商品画像の URL |
| `cart` | 会員ごとのカート本体 |
| `cart_items` | カート内の商品と数量 |
| `order` | 注文ヘッダ |
| `order_items` | 注文明細 |

主な制約や設計ポイントは次の通りです。

- `users.email` は一意
- 会員ごとに `cart` は 1 件まで
- `cart_items` は `cart_id + product_id` の組み合わせで重複禁止
- 注文ステータスは `PENDING`, `PROCESSING`, `SHIPPED`, `DELIVERED`, `CANCELED`
- 商品はカテゴリに任意で紐づけ可能
- `product_images` は画像 URL の保管を想定しており、拡張余地が大きい

## 6. 技術構成

### 6-1. フロントエンド・アプリケーション

- Next.js 16
- React 19
- TypeScript
- App Router
- Tailwind CSS
- shadcn/ui 系コンポーネント

### 6-2. バックエンド・データ層

- Prisma
- PostgreSQL
- Zod（入力バリデーション）
- `bcrypt`（パスワードハッシュ化）
- `jose` / `jsonwebtoken`（JWT 認証）
- Axios（商品検索 API の呼び出し）

### 6-3. コードの配置

- `src/app/**` → ページとルーティング
- `src/app/features/**` → 機能単位の Server Action / Form / UI
- `src/components/**` → 共通ヘッダー、UI、プロバイダー
- `src/lib/**` → JWT、Cookie、Prisma、Supabase 等の共通処理
- `prisma/schema.prisma` → DB スキーマ定義

## 7. 実装済み機能と今後の拡張

### 7-1. 実装済み

- 会員登録、ログイン、ログアウト
- JWT Cookie ベースの認証
- 公開ページ／保護ページのルーティング制御
- 商品一覧の検索、カテゴリ絞り込み、ページネーション
- 商品詳細表示
- カートの追加・更新・削除
- 配送先情報の登録、更新
- 管理者ダッシュボードでの売上・在庫・ユーザー数表示
- 管理者による商品管理と在庫管理

### 7-2. 今後の拡張候補

- 注文確定処理の実装
- 決済サービス連携
- 注文履歴と注文詳細画面
- 管理者による注文ステータス更新
- 複数配送先の管理
- 商品画像アップロードと一覧への表示
- より詳細な売上分析や期間集計
- パスワード変更ページの導線と UX 改善

## 8. 開発起動方法

```bash
pnpm install
pnpm dev
```

開発サーバーは通常 `http://localhost:3000` で起動し、環境変数として `DATABASE_URL` と `JWT_SECRET` が必要です。Supabase を利用する場合は `NEXT_PUBLIC_SUPABASE_URL` と `NEXT_PUBLIC_SUPABASE_ANON_KEY` も必要です。

## 9. まとめ

この EC サイトは、一般会員向けの「商品を見る→カートに入れる→購入へ進む」体験と、管理者向けの「商品と在庫を運用する」体験を一つのアプリケーションとして持つ構成です。

現在の実装は、EC の基本機能である商品管理と購買体験の土台を十分に備えています。一方で、注文確定、決済、配送管理、購入履歴までは今後の実装対象です。

このため、次の優先テーマは「チェックアウトから注文完了までの業務フローを本番向けに接続すること」だと整理できます。
