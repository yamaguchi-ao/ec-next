# ECサイト概要資料

## 1. このサイトの位置づけ

本プロジェクトは、会員が商品を探してカートに追加する購入導線と、管理者が商品・在庫を管理する管理画面を備えた EC サイトです。現時点では、認証、商品検索、カート、商品管理、管理者向け集計までが実装されています。

> この資料はコードベースをもとにした現状整理です。決済・注文確定など、画面上に導線があっても未接続の機能は「今後の拡張」に分けて記載しています。

## 2. 利用者と権限

| 利用者 | できること | 主な入口 |
| --- | --- | --- |
| 一般会員 | 商品検索、商品詳細の確認、カート操作、配送先の登録・更新、パスワード変更 | `/products/list` |
| 管理者 | ダッシュボード確認、商品・カテゴリ・在庫の登録／更新／削除 | `/dashboard` |

ユーザー情報の `admin` フラグで権限を判定します。一般会員が管理者画面へアクセスした場合は商品一覧へ、管理者が一般会員画面へアクセスした場合はダッシュボードへ遷移します。

## 3. 画面構成と画面遷移

### 認証

- `/`：`/login` へリダイレクト
- `/login`：一般会員ログイン
- `/login/admin`：管理者ログイン
- `/signup`：会員登録

会員登録・ログインではメールアドレスと 8 文字以上のパスワードを検証します。パスワードは bcrypt でハッシュ化し、ログイン後は JWT を `httpOnly` の `auth_token` Cookie に保存します。JWT の有効期限は 1 時間です。

### 一般会員向け

- `/products/list`：販売中商品の一覧、商品名検索、カテゴリ絞り込み、ページネーション
- `/products/list/[id]`：商品詳細
- `/cart`：カート内容、数量変更、削除、金額表示
- `/account`：ユーザーの配送先情報
- `/account/password`：ヘッダーからの導線とパスワード変更アクションを用意（専用ページの実装状況は要確認）

未ログインで保護対象ページへアクセスすると `/login` へ遷移します。

### 管理者向け

- `/dashboard`：商品数、会員数、注文数、売上、対応待ち注文、低在庫商品、最近の注文を表示
- `/products/management`：商品検索、一覧、新規登録
- `/products/management/[id]`：商品更新・販売状態変更・削除

管理者の商品検索は商品名またはカテゴリ名を対象とし、1 ページ 10 件で表示します。商品登録時は商品名・カテゴリ・価格・在庫数を検証し、重複商品と重複カテゴリを拒否します。

## 4. 主要な業務フロー

### 一般会員

1. `/signup` で会員登録、または `/login` でログイン
2. 商品名やカテゴリから販売中商品を検索
3. 商品詳細で価格・在庫・説明を確認
4. 数量を指定してカートへ追加
5. `/cart` で数量、送料、合計を確認
6. 購入処理へ進む（購入ボタンは現状 UI のみ）

カート追加時は、販売中か、指定数量が在庫以内かをサーバー側で確認します。同一商品を追加した場合は既存数量に加算されます。

### 管理者

1. `/login/admin` でログイン
2. ダッシュボードで在庫・注文状況を確認
3. 商品管理画面で商品を検索
4. 商品を登録・更新・削除
5. 在庫数と販売中／停止中の状態を更新

## 5. データ構成

データベースは Prisma で PostgreSQL に接続します。

| モデル | 役割 |
| --- | --- |
| `users` | 会員・管理者、認証情報 |
| `address` | 会員の配送先住所・電話番号 |
| `products` | 商品名、価格、在庫数、説明、販売状態 |
| `category` | 商品カテゴリ |
| `product_images` | 商品に紐づく画像 URL（モデル定義あり） |
| `cart` / `cart_items` | 会員ごとのカートと商品数量 |
| `order` / `order_items` | 注文ヘッダと注文明細 |

主な制約は次のとおりです。

- メールアドレスは一意
- 1 会員につきカートは 1 件
- 1 カート内の商品重複は禁止（`cart_id` と `product_id` の複合一意）
- 注文ステータスは `PENDING`、`PROCESSING`、`SHIPPED`、`DELIVERED`、`CANCELED`
- 商品はカテゴリに任意で紐づく

## 6. システム構成

```text
ブラウザ
  └─ Next.js App Router（ページ、Client Component、Server Action）
       ├─ proxy.ts：公開／保護パスと JWT の確認
       ├─ /api/products/list：販売中商品の検索 API
       ├─ Prisma：PostgreSQL へのデータアクセス
       └─ UI：Tailwind CSS + shadcn/ui 系コンポーネント
```

### 技術スタック

- Next.js 16 / React 19 / TypeScript
- Tailwind CSS、shadcn/ui
- Prisma 7、PostgreSQL
- `jose`（JWT）、`bcrypt`（パスワードハッシュ）
- Zod（フォーム入力バリデーション）
- Axios（商品検索 API 呼び出し）
- Supabase クライアントも依存関係・補助実装として存在

### コードの分割方針

- `src/app/**`：ルーティングとページ
- `src/app/features/**`：機能単位の Server Action、フォーム、UI
- `src/components/**`：ヘッダー、プロバイダー、共通 UI
- `src/lib/jwt/**`：JWT と Cookie
- `src/lib/prisma/**`：Prisma クライアントとクエリ
- `prisma/schema.prisma`：データモデル

## 7. 実装済みと今後の拡張

### 実装済み

- 一般会員／管理者のログイン、会員登録、ログアウト
- JWT Cookie による認証と画面単位の権限制御
- 販売中商品の検索、カテゴリ絞り込み、ページネーション
- 商品詳細表示
- カート追加、数量の増減、削除、在庫チェック
- 配送先情報の登録・更新
- 管理者の商品・カテゴリ・在庫・販売状態の管理
- ダッシュボードの件数・売上・低在庫・最近の注文の集計

### 今後の拡張候補

- 購入ボタンから注文を作成するチェックアウト処理
- 決済サービス連携
- 注文履歴と注文詳細、管理者による注文ステータス更新
- 配送先を複数保存する機能
- 商品画像のアップロードと一覧・詳細への表示
- ダッシュボードの推移グラフや期間別集計
- パスワード変更画面の導線確認と整備

## 8. 開発時の起動

```bash
pnpm install
pnpm dev
```

主な設定値は `.env` の `DATABASE_URL` と `JWT_SECRET` です。Supabase を利用する場合は `NEXT_PUBLIC_SUPABASE_URL` と `NEXT_PUBLIC_SUPABASE_ANON_KEY` も設定します。開発サーバー起動後、`http://localhost:3000` にアクセスしてください。

## 9. まとめ

このサイトは、会員が商品を検索してカートに入れるフロント側の購入導線と、管理者が商品・在庫を運用する管理機能を一体化した EC サイトです。データモデルには注文や商品画像の拡張余地も確保されているため、次の優先課題はチェックアウト、決済、注文履歴を業務フローとして接続することです。
